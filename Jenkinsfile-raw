pipeline {
  agent {
    kubernetes {
      yaml """
kind: Pod
metadata:
  name: cloudbees-core
spec:
  containers:
    - name: maven
      image: openjdk:8-jdk-alpine
      command:
        - cat
      tty: true
      workingDir: "/home/jenkins/agent"
"""
    }
  }

  stages {
    stage('Run maven') {
      steps {
             container('jdk') {
              sh './mvnw spring-javaformat:apply  && ./mvnw package'
          }
      }
    }
}
}
