pipeline {
  agent {
    kubernetes {
      yaml """
kind: Pod
metadata:
  name: cloudbees-core
spec:
  containers:
    - name: maven
      image: maven:3.3.9-jdk-8-alpine
      runAsUser: 1000
      command:
        - cat
      tty: true
      workingDir: "/home/jenkins/agent"
      securityContext:
        runAsUser: 1000
"""
    }
  }

  stages {
    stage('Run maven') {
      steps {
             container('maven') {
              sh './mvnw spring-javaformat:apply  && ./mvnw package'
          }
      }
    }
}
}
